<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.gymbro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity1"></activity>
        <activity android:name=".Chest"></activity>
        <activity android:name=".Shoulder"></activity>
        <activity android:name=".Back"></activity>
        <activity android:name=".Legs"></activity>
        <activity android:name=".Biceps"></activity>
        <activity android:name=".Abs"></activity>
        <activity android:name=".GymTime"></activity>
        <activity android:name=".NutritionGym"></activity>

    </application>

</manifest>